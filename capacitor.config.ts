import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.recebimentosmart.app',
  appName: 'Recebimento Smart',
  webDir: 'dist'
};

export default config;
